<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Treatment-Mapper">
	<sql id="search">
		<if test="regdateBefore != null and regdateBefore != '' and regdateAfter != null and regdateAfter != ''">
			and treatment_date BETWEEN to_date(#{regdateBefore},'yyyy-MM-dd') and to_date(#{regdateAfter},'yyyy-MM-dd')
		</if>
	</sql>

	<select id="selectSearchTreatmentList" parameterType="AnimalCarePageMaker" resultType="treatment">
		select *
		from treatment
		where tno is not null
		<include refid="search"/>
		order by regdate desc
	</select>
	<select id="selectTreatmentBytno" parameterType="AnimalCarePageMaker" resultType="treatment">
		select *
		from treatment
		where tno = #{tno}
	</select>
	<insert id="insertTreatment" parameterType="treatment">
		
	</insert>
	
	
</mapper>









